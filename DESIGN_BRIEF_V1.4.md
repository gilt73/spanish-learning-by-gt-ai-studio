# Spanish Learning — בריף עיצוב ל-Claude Design (V1.4)

מסמך זה מיועד להעתקה לתוך פרויקט חדש ב-**claude.ai/design**. המטרה: לבנות ספריית קומפוננטות עקבית לאפליקציה הקיימת, שתסונכרן בחזרה לקוד דרך `DesignSync`.

**חשוב להבין את חלוקת התפקידים:** Claude Design בונה את המראה — HTML/CSS אמיתי, קומפוננטות, states חזותיים. הלוגיקה בפועל (state management, quiz engine, אינטגרציית Spotify, שמירה ל-localStorage) **נשארת ב-Claude Code** ומחוברת אחרי הסנכרון. אז המוקים צריכים להיות "אמיתיים" ברמת ה-HTML/CSS אבל לא חייבים לעבוד פונקציונלית (אפשר data דמה בכל מקום).

---

## 1. סטיילינג — פתוח לגמרי, לא מוכתב

**אין לשמור על העיצוב הנוכחי.** המראה הקיים (רקע כהה, glassmorphism, גרדיאנט כחול-סגול) הוא רק placeholder זמני שנבנה מהר כדי שיהיה פרוטוטייפ עובד — הוא **לא** מגבלה. Claude Design צריך לחקור ולהחליט מה הכי מתאים לאפליקציה מסוג הזה מאפס, בלי להיות כבול לשום פלטת צבעים או סגנון קיים.

**הקשר שכדאי לקחת בחשבון בבחירת הסטיילינג:**
- אפליקציית לימוד שפה (עברית → ספרדית), עם אלמנטים של gamification (streak, XP, quiz) — יש הרבה מה ללמוד מאפליקציות כמו Duolingo מבחינת טון: ידידותי, מעודד, לא מלחיץ
- קהל היעד: דוברי עברית שרוצים ללמוד ספרדית דרך שירי סלסה/בצ'אטה — יש כאן גם זווית של תרבות/מוזיקה/ריקוד שאפשר לבטא ויזואלית (חום, אנרגיה, קצב), לא רק "אפליקציית לימוד" יבשה
- **RTL עברית חובה** (`dir="rtl"`), עם מילים/משפטים בספרדית שתמיד `dir="ltr"` בתוך ההקשר — כל כיוון/סגנון שנבחר צריך לעבוד נכון ב-RTL
- מובייל-פירסט, מסגרת אפליקציה ברוחב מצומצם (feel של אפליקציית טלפון), עם bottom navigation
- האפליקציה חייבת לתמוך גם light וגם dark mode אם הבחירה העיצובית כוללת רקע בהיר

תבחרו טיפוגרפיה, פלטת צבעים, סגנון כרטיסים/כפתורים, ורמת "אנרגיה" ויזואלית לגמרי מחדש, לפי מה שהכי מתאים לתחום הזה.

---

## 2. מלאי קומפוננטות (הדבר הכי חשוב לבנות בעקביות)

כרגע יש חוסר אחידות קל בין מסכים. אלה הקומפוננטות שחוזרות על עצמן וכדאי שיהיו קומפוננטה מוגדרת אחת לכל אחת:

| קומפוננטה | איפה מופיעה היום | מה לתקנן |
|---|---|---|
| **Song card** | בית (המשך), ספרייה | תמונת עטיפה/אימוג'י, כותרת, אמן, badge ז'אנר, מצב locked |
| **Word chip** | שיעור, (בעתיד) מאגר מילים | מילה בספרדית + תרגום, מצב "נחשף" (הגייה גלויה) |
| **Level pill** | בית, הגדרות | 3 מצבים: לא נבחר / נבחר / (אולי) disabled |
| **Progress bar** | שיעור, התקדמות (skill bars) | track אפור + fill בגרדיאנט |
| **Quiz option button** | תרגול | states: ברירת מחדל / נכון (ירוק) / שגוי (אדום) |
| **Bottom nav item** | כולם | active / inactive |
| **Stat tile** (מספר + לייבל) | בית (streak/XP), התקדמות | אייקון + מספר גדול + לייבל קטן |
| **Category card (חדש)** | מאגר מילים (טרם קיים ב-UI) | אייקון, כותרת קטגוריה, badge רמה, מספר מילים |

**בקשה ל-Claude Design**: תבנה "עמוד קומפוננטות" (component sheet) עם כל אחת מהן ב-3 states, לפני שבונים מסכים שלמים. זה מה ש-DesignSync הכי טוב בלסנכרן בהדרגה.

---

## 3. רשימת מסכים

### קיימים (לשפר/להאחיד):

1. **בית** — ברכה + streak/XP, בחירת רמה (3 pills), ייבוא שיר מספוטיפיי (input + חיפוש חי), "המשיכו איפה שעצרתם"
2. **ספריית שירים** — רשימת כרטיסי שיר (חלקם locked)
3. **שיעור (מסך לימוד שורה)** — נגן Spotify embed, משפט בספרדית + תרגום, כפתורי השמעה/הגייה, רשימת word chips, כפתור "המשך"
4. **תרגול (Quiz)** — שאלה, 4 אפשרויות בגריד 2×2, מסך סיום עם ניקוד
5. **התקדמות** — 2 stat tiles (streak/XP), 3 skill progress bars, רשימת שירים שהושלמו
6. **הגדרות** — בחירת רמה, 3 toggles, קופסת גרסה

### חדש לגמרי — צריך לעצב:

7. **מאגר מילים (Word Bank)** — נגיש מתוך "ספרייה" עם טאב/מתג בין "שירים" ל-"מאגר מילים". גריד של category cards (2 עמודות): אייקון, שם קטגוריה (למשל "ברכות והיכרות", "מספרים", "פעלים בסיסיים"), badge רמה, מספר מילים.
8. **פרטי קטגוריית מילים** — אחרי לחיצה על קטגוריה: כותרת + רשימת word chips (אותה קומפוננטה כמו בשיעור) + כפתור "תרגלו קטגוריה זו" שמוביל לאותו מסך Quiz הקיים.

כל 8 המסכים אמורים לצאת **בסגנון החדש האחיד** שתבחרו — לא בהמשך לסגנון הקיים.

---

## 4. מה **לא** לגעת בו (זה כבר עובד ומחובר ללוגיקה)

- שמות ה-`id` של אלמנטים קיימים ב-`index.html` (`#home-streak`, `#lesson-words`, `#quiz-options` וכו') — אם Claude Design משנה מבנה, צריך לתעד בבירור אילו `id`-ים זזו כדי שאני אעדכן את `app.js` בהתאם
- אינטגרציית Spotify (embed iframe, oEmbed, search API) — זה backend/JS, לא עיצוב
- הלוגיקה של quiz, XP, streak — נשארת ב-`app.js`

---

## 5. תוצר מצופה מ-Claude Design

1. כיוון עיצובי מוצע (פלטה, טיפוגרפיה, "אישיות" ויזואלית) עם הסבר קצר למה זה מתאים לאפליקציית לימוד ספרדית לדוברי עברית
2. עמוד קומפוננטות (component sheet) עם כל הפריטים מסעיף 2, ב-states שונים, בסגנון החדש
3. מוקים ל-8 המסכים (6 הקיימים מחדש + 2 החדשים), כולם בסגנון האחיד החדש
4. הכל כ-HTML/CSS אמיתי (לא Figma/תמונה) כדי ש-`DesignSync` יוכל למשוך אותו ישירות לקוד

---

*לאחר שהמוקים מוכנים ב-claude.ai/design, נסנכרן אותם לכאן עם `DesignSync` ואחבר אותם ללוגיקה הקיימת.*
