# Spanish Learning — by G.T AI Studio
### מסמך איפיון (Spec) — גרסה 1.0

אפליקציית מובייל ללימוד ספרדית לדוברי עברית, עם דגש על לימוד דרך שירי סלסה ובצ'טה אמיתיים.

---

## 1. קונספט
המשתמש מדביק קישור לשיר בספוטיפיי (או בוחר שיר מתוך ספרייה מובנית), והאפליקציה מפרקת אותו לשורות, מלמדת מילה-מילה ומשפט-משפט: משמעות, הגייה ודקדוק — הכל בעברית. שלוש רמות לימוד: מתחיל, בינוני, מתקדם.

## 2. מסכים עיקריים

| מסך | תפקיד |
|---|---|
| **בית** | ברכה, streak יומי, בחירת רמה, כפתור "למד עם שיר" |
| **ייבוא שיר** | הדבקת קישור ספוטיפיי / חיפוש שיר / בחירה מספרייה |
| **לימוד שורה** | שורת שיר מפורקת למילים; הגייה, תרגום, השמעה מואטת |
| **תרגול** | שאלות אמריקאיות, השלמת משפט, סידור מילים |
| **הגייה (מיקרופון)** | השוואת הקלטת המשתמש לקריינות מקור |
| **התקדמות** | XP, streak, רמת שליטה לפי נושא, שירים שנלמדו |
| **ספריית שירים** | כל השירים שנלמדו/נשמרו + flashcards שנוצרו מהם |
| **הגדרות** | רמה, שפה, קול, גודל טקסט, מידע גרסה |

## 3. מבנה 3 הרמות

- **מתחיל** — ביטויים נפוצים, אוצר מילים בסיסי (ריקוד/אהבה/גוף), הגייה איטית, תעתיק בעברית.
- **בינוני** — משפטים מלאים, זמן הווה + ציווי, פירוק דקדוקי קצר לכל שורה.
- **מתקדם** — שיר שלם, סלנג קריבי (דומיניקני/קולומביאני), שירה יחד עם הקצב (shadowing), תרגום חופשי בלי רמזים.

## 4. מודל נתונים (טיוטה)

```
Song { id, title, artist, spotifyUrl, level, lines[] }
Line { id, textEs, textHe, words[], audioUrl }
Word { es, he, pronunciationHe, partOfSpeech }
UserProgress { userId, xp, streak, level, songsCompleted[], wordsLearned[] }
```

## 5. מגבלה טכנית חשובה — מילות שירים
Spotify API **לא** מספק מילות שיר (רק metadata + ניגון). כדי לממש "הדבק קישור → קבל מילים מפורקות" בצורה חוקית, יש שתי אפשרויות:
1. אינטגרציה עם ספק מילים מורשה (למשל Musixmatch Developer API — יש מגבלות שימוש ורישוי).
2. במקום פירוק אוטומטי, מאגר שירים שנבחר ונערך ידנית מראש (הכי בטוח מבחינה משפטית, אבל פחות "כל שיר בעולם").

הפרוטוטייפ הנוכחי (גרסה 1.0) משתמש בנתוני **דוגמה מקוריים** (לא מילים אמיתיות של שיר קיים) כדי להדגים את חוויית הלמידה בלי סוגיית זכויות יוצרים. אינטגרציית מילים אמיתית היא הצעד הבא.

### 5.1 מגבלה נוספת — Spotify Web API (מאז נוב׳ 2024)
אפליקציות **חדשות** שנרשמות ב-Spotify Developer Dashboard לא מקבלות יותר גישה ל-`preview_url` (קטע 30 שניות), ל-`recommendations`, ל-`audio-features` ול-`related-artists`. חיפוש שירים ופרטי מטא-דאטה בסיסיים (`search`, `track`, `album`) עדיין עובדים. הרשמה עצמה חינמית לגמרי (אין עלות, אין צורך בכרטיס אשראי).

**הפתרון שנבחר ל-V1.1**: שימוש ב-**Spotify oEmbed API הציבורי** (`https://open.spotify.com/oembed?url=...`) — endpoint פתוח שלא דורש Client ID/Secret או הרשמה בכלל, ומחזיר שם שיר + תמונת עטיפה אמיתיים. לניגון בפועל, embed player רשמי (`https://open.spotify.com/embed/track/{id}`) שגם הוא לא דורש אימות. כך V1.1 עובד **לגמרי בצד לקוח (client-side)**, בלי backend ובלי סודות API — רק חיפוש טקסטואלי מלא (V1.1b, אופציונלי) ידרוש בעתיד הרשמה + שרת proxy קטן שמחזיק את ה-Client Secret.

## 6. סטאק טכנולוגי מומלץ
- **גרסה 1.0 (זו)**: אתר מובייל-פירסט (HTML/CSS/JS וניל, ללא build) — ריצה מיידית, קל לאיטרציה.
- **המשך מומלץ**: React Native + Expo (אפליקציה אמיתית לחנויות), backend קליל (Node/Firebase) לניהול משתמשים, XP, ומאגר שירים; TTS/STT (Web Speech API או Whisper) לתרגול הגייה.

## 7. Roadmap
1. ✅ V1.0 — פרוטוטייפ UI מלא, נתוני דוגמה, ניווט בין כל המסכים.
2. ✅ V1.1 — הדבקת קישור שיר אמיתי → שם שיר, עטיפת אלבום ונגן אמיתיים דרך Spotify oEmbed (ללא הרשמה/סודות). מילה-מילה עדיין רק לשירי הדוגמה.
   - V1.1b (אופציונלי, דורש הרשמה + שרת proxy) — חיפוש טקסטואלי מלא ("הקלידו שם שיר") דרך Spotify Web API המלא.
3. V1.2 — מאגר מילים ראשוני (10-20 שירים שנערכו ידנית).
4. V1.3 — תרגול הגייה עם מיקרופון (Web Speech API).
5. V2.0 — חשבון משתמש, שמירת התקדמות בענן, אפליקציית מובייל אמיתית.
